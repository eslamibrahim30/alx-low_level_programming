#include "main.h"
#include <unistd.h>

/**
 * add - Calculate the sum of two given numbers
 * @a: the first number
 * @b: the second number
 * Return: the sum of a + b
 */
int add(int a, int b)
{
	return (a + b);
}
